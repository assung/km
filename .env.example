# ds-product-template — copy 成 .env(已被 .gitignore 忽略)後填值。
#
# ─── Storybook deploy 的免費密碼保護(npm run deploy:storybook)──────────────
#
# 重要(2026-06 verified):Netlify *dashboard* 的 Password Protection 是 Pro 方案
# ($20/mo)付費功能,free / Starter 沒有。free-tier 真正可用的密碼保護 =
# HTTP Basic Auth via `_headers`。`npm run deploy:storybook` 會在 build 產物
# storybook-static/ 注入 `_headers`(用下面這組帳密),Netlify edge 層用瀏覽器
# 原生帳密彈窗擋住整站。帳密只寫進 build 產物(已 gitignore),永不進版控。
#
# 設了 USER + PASSWORD 才會啟用密碼保護;沒設 = 公開部署(script 會 warn)。
NETLIFY_BASIC_AUTH_USER=
NETLIFY_BASIC_AUTH_PASSWORD=
#
# 要 deploy 到哪個 Netlify site(Admin URL 或 `netlify status` 可查到 site id)。
# 沒設則用已 `netlify link` 的 site。
NETLIFY_SITE_ID=
