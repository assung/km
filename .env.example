# ds-product-template — copy 成 .env(已被 .gitignore 忽略)後填值。
#
# ─── Storybook deploy(npm run deploy:storybook)─────────────────────────────
#
# 要部署到哪個 Netlify site(Admin URL 或 `netlify sites:list` 可查到 site id)。
# 沒設則用已 `netlify link` 的 site。
NETLIFY_SITE_ID=
#
# ─── 密碼保護的帳密放哪?──────────────────────────────────────────────────
#
# ⚠️ 帳密「不放這裡」。密碼保護由 Netlify Edge Function(netlify/edge-functions/basic-auth.ts)
# 負責,它讀的是「Netlify 站台的環境變數」,不是本機 .env:
#
#   到 Netlify dashboard → Site configuration → Environment variables → 新增:
#     BASIC_AUTH_USER       = team(或你要的帳號)
#     BASIC_AUTH_PASSWORD   = 你的共用密碼
#
# 這樣帳密只存在 Netlify 端,永不進 repo / build 產物,最安全。沒設 = 站台公開不擋。
#
# (背景:free 方案不執行 `_headers` Basic-Auth、dashboard Password Protection 是 Pro $20/mo
#  付費;edge function 是 free 方案唯一真正可用的密碼保護方式,2026-06 實測確認。)
